<h1>Home#profile</h1>
<p>Find me in app/views/home/profile.html.erb</p>

    <nav  class="navbar navbar-default">
        <div  class="container-fluid">
            <div  class="navbar-header">
                <button type="button" class="navbar-toggle  collapsed"  data-toggle="collapse" data-target="#collapibleMenu">
                    <span class="icon-bar"></span>
                </button>
                <h1>PERGUNTADOR</h1>
            </div>


            <div  class="collapse navbar-collapse"  id="bs-example-navbar-callapse-1">
              <ul class="nav  navbar-nav  navbar-right">   
               <% if current_user %>
                 	<li >Signed in as:  <%= current_user.name %> - <%= image_tag(@auth['info']['image'], {width: '50', heigth: '50', class: 'img-circle'}) %></li>
                 	<li><%= link_to "Signed Out", 'sign_out', method: :delete %></li>
               <% else %>
                 	<li><%= link_to "auth/facebook" do %><%= image_tag('facebook.jpg', {width: '40', heigth: '40', class: 'img-circle'}) %><% end %></li>
               <% end %>
             </ul>
           </div>
        </div>
     </nav>


       <% flash.each do |key, value| %>
       <div class="alert alert-info">
          <%= value %>
       </div>
       <% end %>

       <% if current_user %>
        <div class="text-center">
          <h1>Olá <%= current_user.name %></h1>
        </div>
       <% end %>
